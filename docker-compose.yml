version: '3.9'
services:
  agent:
    build: .
    container_name: langgraph-agent
    env_file:
      - example.env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped
